// PWAInstallPromptコンポーネントの使用例
// App.tsxに以下のように追加してください:

/*
import { PWAInstallPrompt } from './components/PWAInstallPrompt';

function App() {
  // ... 既存のコード ...

  return (
    <div className="min-h-screen bg-gray-50">
      {/* 既存のコンテンツ */}
      
      {/* PWAインストールプロンプトを追加 */}
      <PWAInstallPrompt />
    </div>
  );
}
*/

// または、特定のページでのみ表示したい場合:

/*
{currentPage === 'main' && <PWAInstallPrompt />}
*/
